# OpenNext on Cloudflare Workers can't follow pnpm's symlinked node_modules
# layout when copying packages into the bundle (e.g. pg's nested pg-cloudflare
# symlink loses its dist/ files). Use the hoisted/flat layout so each package
# is a real directory.
node-linker=hoisted
